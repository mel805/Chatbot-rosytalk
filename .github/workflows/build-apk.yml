name: Build Android APK

permissions:
  contents: write

on:
  push:
    branches:
      - main
    tags:
      - 'v*'
      - 'build-*'
  pull_request:
    branches:
      - main
  workflow_dispatch:
    inputs:
      tag:
        description: "Tag/release to upload APKs to (e.g. build-2025-12-12-xxxx or v5.5.1). Leave empty to use current tag ref."
        required: false
        default: ""
      prerelease:
        description: "Mark GitHub release as prerelease (true/false)."
        required: false
        default: "false"

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Fetch llama.cpp (native local LLM)
      shell: bash
      run: |
        set -euo pipefail
        LLAMA_DIR="app/src/main/cpp/llama.cpp"
        LLAMA_COMMIT="7bed317f5351eba037c2e0aa3dce617e277be1c4"
        if [ ! -d "${LLAMA_DIR}/.git" ]; then
          echo "Fetching llama.cpp into ${LLAMA_DIR}..."
          rm -rf "${LLAMA_DIR}"
          mkdir -p "${LLAMA_DIR}"
          git init "${LLAMA_DIR}"
          git -C "${LLAMA_DIR}" remote add origin https://github.com/ggerganov/llama.cpp
          git -C "${LLAMA_DIR}" fetch --depth 1 origin "${LLAMA_COMMIT}"
          git -C "${LLAMA_DIR}" checkout --detach FETCH_HEAD
          echo "llama.cpp checked out at ${LLAMA_COMMIT}"
        else
          echo "llama.cpp already present, skipping fetch"
        fi
      
    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        
    - name: Setup Android SDK
      uses: android-actions/setup-android@v3
      
    - name: Setup Gradle
      uses: gradle/actions/setup-gradle@v3
      with:
        gradle-version: 8.2
        
    - name: Grant execute permission for gradlew
      run: chmod +x gradlew
      
    - name: Create google-services.json from example
      run: |
        if [ ! -f app/google-services.json ]; then
          echo "Creating google-services.json from example template"
          cp app/google-services.json.example app/google-services.json
        fi
      
    - name: Build Debug APK
      run: ./gradlew assembleDebug --stacktrace --no-daemon
      
    - name: Build Release APK
      run: ./gradlew assembleRelease --stacktrace --no-daemon
      
    - name: Upload Debug APK
      uses: actions/upload-artifact@v4
      with:
        name: app-debug
        path: app/build/outputs/apk/debug/app-debug.apk
        
    - name: Upload Release APK
      uses: actions/upload-artifact@v4
      with:
        name: app-release
        path: |
          app/build/outputs/apk/release/*.apk
        
    - name: Create Release and Upload APK
      if: startsWith(github.ref, 'refs/tags/') || (github.event_name == 'workflow_dispatch' && inputs.tag != '')
      uses: softprops/action-gh-release@v2
      with:
        tag_name: ${{ github.ref_type == 'tag' && github.ref_name || inputs.tag }}
        fail_on_unmatched_files: false
        files: |
          app/build/outputs/apk/debug/app-debug.apk
          app/build/outputs/apk/release/*.apk
        draft: false
        prerelease: ${{ (github.ref_type == 'tag' && startsWith(github.ref_name, 'build-')) || inputs.prerelease == 'true' }}
        generate_release_notes: true
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
